Basic application demonstrating Spring Boot / Spring MVC / Thymleaf / REST / Spring Test.

Demonstrates using the Spring Test MockMvc framework for ease of unit testing controllers, and Spring's TestRestTemplate for integration testing the controllers.

The back end data is hardcoded for this demo.

Run with:

mvn spring-boot:run -Drun.jvmArguments="-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=5005"

(Optionally debug on port 5005).

Navigate to URL:

http://localhost:8080/airport/list